- args:
    name: dispositions
    table:
      name: agencies
      schema: public
    using:
      foreign_key_constraint_on:
        column: agency_id
        table:
          name: dispositions
          schema: public
  type: create_array_relationship
